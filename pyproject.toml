[project]
name = "spectral-comparison"
version = "0.1.0"
description = "Analysis of intractranial and reconstructed spectral features"
readme = "README.md"
requires-python = ">=3.14.5"
dependencies = [
    "mne>=1.12",
    "polars",
    "plotnine",
    "numpy<2.5",
    "specparam>=2.0.0rc7",
    "pyrasa",
    "pesco",
    "seaborn",
    "great-docs",
    "autoreject",
    "neurodsp",
    "patchworklib",
    "nibabel",
    "nilearn",
    "pyvistaqt",
    "ipywidgets",
    "ggsegpy",
    "atlasreader",
    "pymc",
    "bambi",
    "jupyter-cache",
    "ridgenine",
    "duckdb",
    "plotnine-extra",
    "lisc"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "variable-inspector",
    "ruff",
    "jupytext",
    "ipykernel",
    "pip",
    "notebook",
    "scipy-stubs",
    "ipympl",
   "nbstripout",
   "pre-commit",
   "watermark",
   "mlxtend"
    
]

[tool.uv]
package = false
link-mode = "copy"
config-settings = { editable_mode = "compat" }

[tool.uv.workspace]
members = [
    "pesco",
]

[tool.uv.sources]
pesco = { workspace = true, editable = true }
gedai = { git = "https://github.com/neurotuning/gedai" }
ggsegpy = { git = "https://github.com/ggsegverse/ggsegpy.git" }
ridgenine = { git = "https://github.com/briandconnelly/ridgenine" }

[tool.ruff]
line-length = 88
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = []

[tool.black]
line-length = 88
target-version = ['py314']

